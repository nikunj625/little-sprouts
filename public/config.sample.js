// Copy this file to config.js and fill in your values for LOCAL dev.
// On GitHub Pages, config.js is generated automatically by the deploy Action
// from your repository Variables (Settings → Variables → Actions).
//
// Get these from: Supabase → Project Settings → API
window.LS_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
  VAPID_PUBLIC_KEY: "" // optional — for closed-app push reminders
};
